<div class="min-h-screen bg-gradient-to-br from-purple-600 via-indigo-600 to-blue-500 flex items-center justify-center px-4 py-12 sm:px-6 lg:px-8">
  <div class="w-full max-w-md animate-slide-up">
    <!-- Brand -->
    <div class="text-center mb-8">
      <div class="flex items-center justify-center gap-3 mb-2">
        <span class="text-4xl bg-white/20 backdrop-blur-sm p-3 rounded-2xl shadow-lg">💰</span>
        <h2 class="text-3xl font-bold text-white tracking-tight">Expense Tracker</h2>
      </div>
      <p class="text-indigo-100 text-sm font-light">Forgot your password?</p>
    </div>

    <!-- Card -->
    <div class="bg-white/10 backdrop-blur-lg rounded-2xl shadow-2xl p-8 border border-white/20">
      <!-- Flash Messages -->
      <%#= render "shared/flash_messages" %>

      <!-- Sign In Form -->
      <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "space-y-5", data: { turbo: false } }) do |f| %>
        
        <!-- Email Field -->
        <div>
          <%= f.label :email, class: "block text-sm font-medium text-white mb-1.5" do %>
            <span class="flex items-center gap-2">
              <i class="fas fa-envelope text-indigo-200"></i>
              Email Address
            </span>
          <% end %>
          <%= f.email_field :email, 
              class: "w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-white/30 focus:border-transparent transition-all duration-200",
              placeholder: "john@example.com",
              autofocus: true,
              autocomplete: "email",
              required: true %>
        </div>

        <!-- Submit Button -->
        <%= f.submit "Send me password reset instructions", class: "w-full py-3.5 px-4 bg-white text-indigo-600 font-semibold rounded-xl hover:bg-indigo-50 hover:shadow-lg transform hover:scale-[1.02] transition-all duration-200 cursor-pointer focus:outline-none focus:ring-2 focus:ring-white/30" %>
      <% end %>
    </div>
  </div>
</div>
