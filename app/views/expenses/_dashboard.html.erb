<!-- app/views/expenses/_dashboard.html.erb -->

<div class="space-y-6">

  <!-- Statistics -->
  <%= render "month_stats" %>

  <!-- Top month navigation -->
  <%= render "month_navigation" %>

  <!-- Main content -->
  <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">

    <!-- Category Breakdown -->
    <%= render "category_breakdown" %>

    <!-- Expense List -->
    <%= render "expense_list" %>

  </div>

</div>